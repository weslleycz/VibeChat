import { StatusBar } from "@capacitor/status-bar";
import "../globals.scss";

const Home: React.FC = () => {
  StatusBar.setBackgroundColor({ color: "#68E1FD" });
  return (
    <>ghggh</>
  );
};

export default Home;
